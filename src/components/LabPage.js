import styled from 'styled-components'

export default styled.div`
  margin: auto;

  h1 {
    color: #0000ff;
    font-size: 14vmin;
    font-weight: normal;
    line-height: 0.7em;
    text-transform: capitalize;
  }
`
